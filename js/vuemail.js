// create Vue app
const { createApp } = Vue;

const endpoint = "https://flynn.boolean.careers/exercises/api/random/mail";

const app = Vue.createApp({
    data() {
        return {

        }
    }
});
app.mount("#root");